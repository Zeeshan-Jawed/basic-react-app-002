import React from 'react'

function Framework(props){
    return (
    <div>
        <h1>{props.library} is Library</h1>
        <h1>{props.frameWork} is Framework</h1>
    </div>
    )
}
export default Framework;